```javascript
// 批量修改文件名
fs.readdirSync('src/care/entry/img').forEach(file => fs.renameSync(path.resolve(`src/care/entry/img/${file}`), path.resolve(`src/care/entry/img/${file.replace(/_\d\d\d\d_/, '-')}`)))
```

